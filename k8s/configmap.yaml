apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init
  namespace: resume
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS visits (
        id          SERIAL PRIMARY KEY,
        visited_at  TIMESTAMP DEFAULT NOW()
    );